Two small edits to `src/components/Projects.tsx`:

1. **Remove the Markov Chain Worklist Engine link** — Delete the `link` property from the "Markov Chain Worklist Engine" project object so it renders as an unlinked row (no icon, no hover link behavior).

2. **Relabel the section heading** — Change the section label from `/ work` to `/ portfolio`.